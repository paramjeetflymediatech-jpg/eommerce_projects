import { sendOTPEmail, sendWelcomeEmail, sendOrderConfirmationEmail } from "../src/lib/mailer";

// This is just to satisfy the import if needed, but we'll use a script to generate the HTML.
// Actually, I'll just create a script that outputs the HTML to a file.
